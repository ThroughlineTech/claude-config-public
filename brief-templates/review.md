# Brief: Generate Verification Checklist for {ID}

> **For the human, when you come back to this:** This is a delegation brief generated by Claude Code's `/ticket-delegate {ID} review`. To execute it, open Copilot Chat in this project, select your model, and run `/run-brief {brief-path}`. When the agent finishes, come back to Claude Code and run `/ticket-collect {ID}`.

## Your role

You are presenting a completed ticket implementation for human review. Your job is to write a **human-testable verification checklist** the user can follow to confirm the work meets the acceptance criteria. You are NOT modifying code or running tests — read-only on the codebase.

## Ticket

**ID:** {ID}
**Title:** {TITLE}
**Branch:** `{BRANCH}`

### Description
{DESCRIPTION}

### Acceptance Criteria
{ACCEPTANCE_CRITERIA}

### Implementation Plan (what was supposed to happen)
{IMPLEMENTATION_PLAN}

### Files Changed (what actually happened)
{FILES_CHANGED}

### Diff summary
```
{DIFF_SUMMARY}
```

## What to do

1. Read every file in "Files Changed" — both the new version on the branch and (if you can) the original on main, so you understand what changed.
2. For each Acceptance Criterion, design a concrete verification step the human can follow.
3. Add edge-case checks for anything the implementation touches that could break.
4. Add regression checks for adjacent functionality that shares code paths with the changes.
5. Write the checklist into the ticket file at `{TICKET_PATH}`, into the "## Verification Checklist (for human)" section.

### Checklist quality requirements

Each item must be:
- **Specific**: "Navigate to /projects/123 and click 'Start'" not "Test the feature"
- **Observable**: Describe what the user should SEE, not what the code does internally
- **Independent**: Each item can be verified on its own
- **Ordered**: Steps flow naturally (build, then launch, then exercise, then tear down)

### Structure

```markdown
## Verification Checklist (for human)

### Setup
- [ ] Build the project: `{BUILD_CMD}`
- [ ] {any prerequisite state, e.g. "Sign in as admin"}

### Core Functionality
- [ ] Step 1: Do X, expect to see Y
- [ ] Step 2: Do X, expect to see Y
...

### Edge Cases
- [ ] Step N: Try X with invalid input, expect error message Y
...

### Regression Checks
- [ ] Existing feature A still works: do X, see Y
- [ ] Existing feature B still works: do X, see Y
```

## Hard rules

- DO NOT modify any source code
- DO NOT run tests or build (the implementer already did; this is paperwork only)
- DO NOT change the ticket status
- The checklist must cover EVERY acceptance criterion. No skipping.
- Include regression checks for any area of the app that was touched.

## When done

Output: `Brief executed: {brief-filename}. Hand back to Claude Code with: /ticket-collect {ID}`
