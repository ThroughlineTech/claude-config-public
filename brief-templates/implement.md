# Brief: Implement {ID}

> **For the human, when you come back to this:** This is a delegation brief generated by Claude Code's `/ticket-delegate {ID} implement`. To execute it, open Copilot Chat in this project, select your model, and run `/run-brief {brief-path}`. When the agent finishes, come back to Claude Code and run `/ticket-collect {ID}`.

## Your role

You are implementing an approved ticket. The branch has already been created for you. You will work through the Implementation Plan, write tests, run tests/build, commit, and push. You will NOT merge to main and you will NOT deploy — those are separate steps the human will trigger later.

## Ticket

**ID:** {ID}
**Title:** {TITLE}
**Branch:** `{BRANCH}` (already created and checked out)

### Description
{DESCRIPTION}

### Acceptance Criteria
{ACCEPTANCE_CRITERIA}

## Project rules (from CLAUDE.md)

{PROJECT_RULES}

## Project commands (from .claude/ticket-config.md)

- **Test:** `{TEST_CMD}`
- **Build:** `{BUILD_CMD}`
- **Lint:** `{LINT_CMD}`

## Files you'll likely touch

{RELEVANT_FILES}

## Implementation Plan (from the ticket)

{IMPLEMENTATION_PLAN}

## What to do

1. Verify you're on branch `{BRANCH}`. If not, stop and report — do not switch branches yourself.
2. Verify the working tree is clean. If not, stop and report.
3. Work through the Implementation Plan above, item by item, in order. For each item:
   - Read every file you're about to modify before editing it
   - Make the change
   - Move to the next item
4. Add tests following the project's existing test conventions:
   - Test happy path and error cases
   - Test edge cases relevant to the change
   - If a test you didn't write fails, that's a regression — fix it
5. Run the Test command. ALL tests must pass. If any fail: fix and rerun.
6. Run the Build command. Must complete cleanly. If errors: fix and rerun.
7. Stage relevant files specifically (`git add path/to/file`, NOT `git add -A` or `git add .`)
8. Commit with the exact message format: `{ID}: {short description of what changed}`
   - Multiple logical units → multiple commits, each prefixed with `{ID}:`
9. Push the branch: `git push -u origin {BRANCH}` (or `git push` if upstream is set)

## Hard rules

- DO NOT merge to main
- DO NOT deploy
- DO NOT change branches
- DO NOT touch files unrelated to this ticket
- DO NOT modify other tickets' files
- Every commit must be atomic and buildable
- If you discover the Implementation Plan won't work, STOP. Report what you found and what you'd recommend instead. Do not ship a broken or different implementation.

## When done

Output: `Brief executed: {brief-filename}. Hand back to Claude Code with: /ticket-collect {ID}`

In your output, include:
- Number of commits made
- Number of files changed
- Number of tests added
- "All tests passing: yes/no"
- "Build clean: yes/no"

The human (or Claude Code's `/ticket-collect`) will use this to update the ticket's Files Changed and Test Report sections.
